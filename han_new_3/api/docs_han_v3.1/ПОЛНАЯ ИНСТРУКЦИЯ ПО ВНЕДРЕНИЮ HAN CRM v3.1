# 🚀 HAN CRM v3.1 - ПОЛНАЯ ИНСТРУКЦИЯ ПО ВНЕДРЕНИЮ

## 📁 СТРУКТУРА ПРОЕКТА

```
obmensuzak.com.kg/
│
├── api/                              # Backend (PHP)
│   ├── config.php                    ✅ Готов
│   ├── autoload.php                  ✅ Готов
│   ├── .htaccess                     ✅ Готов
│   │
│   ├── classes/                      # PHP классы
│   │   ├── Database.php              ✅ Готов
│   │   ├── Auth.php                  ✅ Готов
│   │   ├── Response.php              ✅ Готов
│   │   ├── Middleware.php            ✅ Готов
│   │   ├── OverpaymentService.php    🆕 НОВЫЙ
│   │   ├── SalesService.php          🆕 НОВЫЙ
│   │   └── StockService.php          🆕 НОВЫЙ
│   │
│   ├── auth.php                      ✅ Готов
│   ├── sales.php                     🆕 ПЕРЕДЕЛАН (использует сервисы)
│   ├── payments.php                  🆕 ПЕРЕДЕЛАН (использует сервисы)
│   ├── clients.php                   ⚠️ НУЖНО УПРОСТИТЬ
│   ├── products.php                  ⚠️ НУЖНО УПРОСТИТЬ
│   └── production.php                ⚠️ НУЖНО УПРОСТИТЬ
│
└── assets/                           # Frontend
    ├── css/
    │   └── style.css
    │
    ├── js/
    │   ├── app.js                    🆕 ГЛАВНЫЙ ФАЙЛ (модульный)
    │   │
    │   ├── config/
    │   │   └── api.config.js         🆕 Настройки API
    │   │
    │   ├── services/                 🆕 Взаимодействие с API
    │   │   ├── api.service.js
    │   │   ├── sales.service.js
    │   │   ├── payments.service.js   ⚠️ НУЖНО СОЗДАТЬ
    │   │   └── clients.service.js    ⚠️ НУЖНО СОЗДАТЬ
    │   │
    │   ├── components/               🆕 UI компоненты
    │   │   ├── Toast.js
    │   │   ├── Modal.js              ⚠️ НУЖНО СОЗДАТЬ
    │   │   └── Loader.js             ⚠️ НУЖНО СОЗДАТЬ
    │   │
    │   └── utils/                    🆕 Утилиты
    │       ├── format.js             ⚠️ НУЖНО СОЗДАТЬ
    │       └── validation.js         ⚠️ НУЖНО СОЗДАТЬ
    │
    └── index.html                    ✅ Готов (добавить type="module")
```

---

## 🔧 ШАГ 1: НАСТРОЙКА БАЗЫ ДАННЫХ

### 1.1 Создание БД

```sql
-- Выполнить в MySQL
SOURCE /path/to/1_XAN_NEW_3.1.sql;
```

### 1.2 Проверка таблиц

```sql
-- Должны быть созданы 13 таблиц:
SHOW TABLES;

-- Проверить переплаты
SELECT * FROM client_overpayments LIMIT 5;
```

---

## 🔧 ШАГ 2: УСТАНОВКА PHP СЕРВИСОВ

### 2.1 Загрузить новые файлы

Скопируйте в `api/classes/`:

- `OverpaymentService.php` ✅
- `SalesService.php` ✅
- `StockService.php` ✅

### 2.2 Обновить контроллеры

Замените файлы:

- `api/sales.php` → новая версия (использует `SalesService`)
- `api/payments.php` → новая версия (использует `SalesService`)

### 2.3 Проверка autoload

Убедитесь, что `autoload.php` загружает все классы:

```php
// api/autoload.php
spl_autoload_register(function ($class) {
    $classFile = __DIR__ . '/classes/' . $class . '.php';
    if (file_exists($classFile)) {
        require_once $classFile;
    }
});
```

---

## 🔧 ШАГ 3: НАСТРОЙКА FRONTEND

### 3.1 Обновить index.html

Добавьте поддержку ES6 модулей:

```html
<!-- В конце <body> -->
<script type="module" src="assets/js/app.js"></script>
```

### 3.2 Создать структуру папок

```bash
cd assets/js/

mkdir -p config services components utils

# Скопировать файлы
cp api.config.js config/
cp api.service.js services/
cp sales.service.js services/
cp Toast.js components/
cp app.js .
```

### 3.3 Проверить импорты

Все файлы должны использовать ES6 синтаксис:

```javascript
// Правильно ✅
import { ApiService } from './services/api.service.js';
export class SalesService { ... }

// Неправильно ❌
const ApiService = require('./api.service.js');
module.exports = { SalesService };
```

---

## 🧪 ШАГ 4: ТЕСТИРОВАНИЕ

### 4.1 Тест создания продажи

**API запрос:**

```bash
curl -X POST https://obmensuzak.com.kg/han/api/sales.php?action=create \
  -H "Content-Type: application/json" \
  -d '{
    "client_id": 1,
    "items": [
      {
        "product_id": 1,
        "quantity": 10,
        "price": 191
      }
    ],
    "paid": 2000
  }'
```

**Ожидаемый ответ:**

```json
{
  "success": true,
  "data": {
    "id": 1,
    "receipt_number": "000001",
    "total": "1910.00",
    "paid": "2000.00",
    "debt": "0.00",
    "new_overpayment": "90.00"
  }
}
```

### 4.2 Проверка баланса клиента

```sql
-- Должна появиться запись в client_overpayments
SELECT * FROM client_overpayments WHERE client_id = 1;

-- У клиента должна быть переплата
SELECT current_overpayment FROM clients WHERE id = 1;
-- Результат: 90.00
```

### 4.3 Тест удаления продажи

```bash
curl -X DELETE https://obmensuzak.com.kg/han/api/sales.php?action=delete&id=1
```

**Проверка:**

```sql
-- Товар вернулся на склад
SELECT quantity FROM stock WHERE product_id = 1;

-- Переплата убрана
SELECT current_overpayment FROM clients WHERE id = 1;
-- Результат: 0.00

-- В истории есть корректировка
SELECT * FROM client_overpayments WHERE client_id = 1 ORDER BY created_at DESC LIMIT 1;
-- type = 'adjusted', amount < 0
```

---

## 🎯 ШАГ 5: МИГРАЦИЯ СТАРЫХ ДАННЫХ

### 5.1 Экспорт из старой БД

```sql
-- Экспорт клиентов
SELECT * FROM old_db.clients INTO OUTFILE '/tmp/clients.csv';

-- Экспорт товаров
SELECT * FROM old_db.products INTO OUTFILE '/tmp/products.csv';
```

### 5.2 Импорт в новую БД

```sql
-- Импорт клиентов
LOAD DATA INFILE '/tmp/clients.csv' 
INTO TABLE clients
FIELDS TERMINATED BY ',' 
LINES TERMINATED BY '\n';

-- Импорт товаров
LOAD DATA INFILE '/tmp/products.csv' 
INTO TABLE products
FIELDS TERMINATED BY ',' 
LINES TERMINATED BY '\n';
```

### 5.3 Пересчёт балансов

```php
// Запустить скрипт пересчёта
$overpaymentService = new OverpaymentService();

foreach ($clients as $client) {
    $overpaymentService->recalculate($client['id']);
}
```

---

## 📊 ПРИМЕРЫ API ЗАПРОСОВ

### Создание продажи

```javascript
const response = await fetch('https://obmensuzak.com.kg/han/api/sales.php?action=create', {
  method: 'POST',
  credentials: 'include',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    client_id: 1,
    items: [
      { product_id: 1, quantity: 10, price: 191 },
      { product_id: 2, quantity: 5, price: 520 }
    ],
    paid: 5000
  })
});

const data = await response.json();
console.log(data);
```

### Добавление платежа

```javascript
const response = await fetch('https://obmensuzak.com.kg/han/api/payments.php?action=create', {
  method: 'POST',
  credentials: 'include',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    sale_id: 1,
    amount: 1000,
    payment_method: 'cash',
    note: 'Погашение долга'
  })
});
```

### Выдача переплаты

```javascript
const response = await fetch('https://obmensuzak.com.kg/han/api/clients.php?action=withdraw-overpayment&id=1', {
  method: 'POST',
  credentials: 'include',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    amount: 50,
    note: 'Выдано наличными'
  })
});
```

---

## ⚠️ ВАЖНЫЕ МОМЕНТЫ

### 1. Транзакции

Все операции изменения баланса должны быть в транзакциях:

```php
$this->db->beginTransaction();
try {
    // ... операции
    $this->db->commit();
} catch (Exception $e) {
    $this->db->rollback();
    throw $e;
}
```

### 2. Баланс клиента

Всегда вычисляется из `client_overpayments`:

```php
// ❌ НЕПРАВИЛЬНО:
$client['current_overpayment'] + $amount

// ✅ ПРАВИЛЬНО:
$overpaymentService->create($clientId, $amount);
$newBalance = $overpaymentService->getBalance($clientId);
```

### 3. Удаление продажи

Всегда делает полный откат:

- Возврат товаров на склад
- Корректировка переплат
- Soft delete связанных записей

---

## 🐛 ОТЛАДКА

### Проверка логов

```bash
# PHP ошибки
tail -f api/logs/php_errors.log

# Apache/Nginx ошибки
tail -f /var/log/apache2/error.log
```

### Проверка транзакций

```sql
-- Все ли транзакции закоммичены?
SHOW ENGINE INNODB STATUS;
```

### Проверка баланса клиента

```sql
-- Пересчитать вручную
SELECT 
    client_id,
    SUM(amount) as calculated_balance,
    (SELECT current_overpayment FROM clients WHERE id = co.client_id) as stored_balance
FROM client_overpayments co
GROUP BY client_id
HAVING calculated_balance != stored_balance;
```

---

## 🎓 ДАЛЬНЕЙШИЕ ШАГИ

1. Создать остальные сервисы:
   - `PaymentsService.js` (фронтенд)
   - `ClientsService.js` (фронтенд)

2. Добавить UI компоненты:
   - `Modal.js`
   - `Loader.js`
   - `DataTable.js`

3. Разделить app.js на страницы:
   - `pages/Dashboard.js`
   - `pages/Sales.js`
   - `pages/Clients.js`

4. Добавить роутинг:
   - `router/router.js`

---

## 📞 ПОДДЕРЖКА

При возникновении проблем проверьте:

1. ✅ Все ли сервисы созданы?
2. ✅ Правильно ли настроен autoload?
3. ✅ Работают ли транзакции?
4. ✅ Есть ли доступ к БД?
5. ✅ Включены ли CORS заголовки?

---

## ✨ ПРЕИМУЩЕСТВА НОВОЙ АРХИТЕКТУРЫ

### Было (старая версия):

- ❌ Вся логика в триггерах MySQL
- ❌ Невозможно отследить ошибки
- ❌ Рассинхронизация данных
- ❌ Сложно тестировать
- ❌ Монолитный app.js на 2000 строк

### Стало (новая версия):

- ✅ Логика в PHP сервисах (понятно и управляемо)
- ✅ Полный контроль над транзакциями
- ✅ История всех операций в БД
- ✅ Легко тестировать каждый метод
- ✅ Модульный фронтенд (легко поддерживать)
- ✅ Возможность восстановления данных
- ✅ Атомарные операции (откат при ошибке)

---

🚀 **Удачи в развёртывании!**